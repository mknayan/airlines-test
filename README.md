This is a React Application.

## To run the Script, simply run the following

In the project directory, run:

### `npm install`

After finishing, just run

### `npm start`

## To Test the project, run

### `npm test`

To create a optimized build production file, configure packages.json homepage link. Then run

### `npm run build`
